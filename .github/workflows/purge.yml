name: Purge jsDelivr Cache

on:
  push:
    branches:
      - main
    paths:
      - 'font/**'
      - 'assets/**'
      - 'meyveler/**'
      - 'music/**'

jobs:
  purge:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get changed files
        id: changes
        run: |
          echo "📝 Checking for changed files in the last commit..."
          # Son commit'te değişen dosyaları al
          CHANGED_FILES=$(git log -1 --name-only --pretty=format: | grep -E '^(font|assets|meyveler|music)/' | sort -u)
          echo "changed_files=$CHANGED_FILES" >> $GITHUB_OUTPUT
          echo "Changed files: $CHANGED_FILES"

      - name: Save changed files to a file
        if: steps.changes.outputs.changed_files != ''
        run: |
          echo "💾 Saving changed files to changed_files.txt..."
          mkdir -p logs
          echo "${{ steps.changes.outputs.changed_files }}" | tr ' ' '\n' > logs/changed_files.txt
          echo "Saved at logs/changed_files.txt"

      - name: Purge jsDelivr cache for changed folders
        if: steps.changes.outputs.changed_files != ''
        run: |
          echo "🧹 Purging jsDelivr cache for changed folders..."
          for FILE in ${{ steps.changes.outputs.changed_files }}; do
            DIR=$(dirname "$FILE")
            echo "$DIR" >> /tmp/dirs_to_purge.txt
          done
          sort -u /tmp/dirs_to_purge.txt | while read DIR; do
            echo "🚀 Purging cache for $DIR..."
            curl -s -X GET "https://purge.jsdelivr.net/gh/buraksariguzel81/buraksariguzeldev@main/$DIR/" \
            && echo "✅ Cache purge completed for $DIR" \
            || echo "⚠️ Failed to purge $DIR"
          done

      - name: Check purge status
        run: |
          echo "✅ jsDelivr cache purge workflow completed successfully!"
          echo "Repository: buraksariguzel81/buraksariguzeldev"
          echo "Branch: main"
          echo "Purged folders:"
          sort -u /tmp/dirs_to_purge.txt
          echo "Changed files saved at logs/changed_files.txt"
